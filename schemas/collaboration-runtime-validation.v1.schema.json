{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentic-graph.dev/schemas/collaboration-runtime-validation/v1",
  "title": "agentic-graph Collaboration Runtime Validation v1",
  "oneOf": [
    {
      "$ref": "#/$defs/success"
    },
    {
      "$ref": "#/$defs/failure"
    }
  ],
  "$defs": {
    "success": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "status", "schemaId", "schemaVersion", "sourceRevision", "reportDigest", "input"],
      "properties": {
        "schema": {
          "const": "agentic-graph.collaboration-runtime-validation/v1"
        },
        "status": {
          "const": "passed"
        },
        "schemaId": {
          "const": "https://agentic-graph.dev/schemas/collaboration-runtime-report/v1"
        },
        "schemaVersion": {
          "const": "agentic-graph.collaboration-runtime-report/v1"
        },
        "sourceRevision": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "reportDigest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "input": {
          "enum": ["stdin", "file"]
        }
      }
    },
    "failure": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "status", "input", "error"],
      "properties": {
        "schema": {
          "const": "agentic-graph.collaboration-runtime-validation/v1"
        },
        "status": {
          "const": "failed"
        },
        "input": {
          "enum": ["stdin", "file", "unknown"]
        },
        "error": {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message"],
          "properties": {
            "code": {
              "enum": [
                "invalid-arguments",
                "invalid-json",
                "schema-validation-failed",
                "input-read-failed",
                "validation-failed"
              ]
            },
            "message": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    }
  }
}
